# LangGraph Basic Example

This project demonstrates a simple workflow using **LangGraph** with a typed state.

## Overview

The graph consists of two nodes:

1. **Greeting Node** (`greet_node`)
   - Receives a user's name.
   - Creates a greeting message.

2. **Uppercase Node** (`uppercase_node`)
   - Takes the greeting generated by the first node.
   - Converts the greeting to uppercase.

### Flow

```text
Input Name
     │
     ▼
 greet_node
     │
     ▼
 uppercase_node
     │
     ▼
 Final Output
```

## Code Structure

### State Definition

```python
class MyState(TypedDict):
    name: str
    greeting: str
```

The graph state contains:

- `name` → User's name
- `greeting` → Generated greeting message

### Node 1: Greeting Node

```python
def greet_node(state: MyState):
    return {
        "greeting": f"Hello {state['name']}"
    }
```

Example:

```python
{"name": "Yash"}
```

Output:

```python
{"greeting": "Hello Yash"}
```

### Node 2: Uppercase Node

```python
def uppercase_node(state: MyState):
    return {
        "greeting": state["greeting"].upper()
    }
```

Input:

```python
{"greeting": "Hello Yash"}
```

Output:

```python
{"greeting": "HELLO YASH"}
```

## Graph Construction

```python
builder = StateGraph(MyState)

builder.add_node("greet", greet_node)
builder.add_node("uppercase", uppercase_node)

builder.set_entry_point("greet")
builder.add_edge("greet", "uppercase")
builder.set_finish_point("uppercase")

graph = builder.compile()
```

Graph sequence:

```text
greet → uppercase
```

## Running the Graph

```python
result = graph.invoke(
    {
        "name": "Yash"
    }
)
```

## Expected Output

```text
Running Greeting Node...
Running Uppercase Node...

Final State:
{
    'name': 'Yash',
    'greeting': 'HELLO YASH'
}
```

## Installation

Install the required package:

```bash
pip install langgraph
```

## Learning Objectives

This example demonstrates:

- Creating a state using `TypedDict`
- Defining LangGraph nodes
- Passing state between nodes
- Building a sequential workflow
- Executing a graph with `invoke()`
- Updating state across multiple nodes

## Author

Basic LangGraph workflow example for learning graph-based AI application development.
